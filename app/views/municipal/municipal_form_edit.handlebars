<div class="container">
  <div class="row">
    <div class="col-md-12" style="margin-top: 5em;">
        <p class="text-center">
          <b>Formulario Municipio</b>
        </p>
        <form id="municipioForm" action="/municipal/edit/{{data.ID}}?_method=PUT" method="POST" class="form-horizontal" role="form">
          <div class="form-group">
            <label for="nombre" class="col-sm-2 control-label">Nombre</label>
            <div class="col-sm-10">
              <input type="text" class="form-control" id="nombre" name="nombre" value="{{data.NOMBRE}}">
            </div>
          </div>
          <div class="form-group">
            <label for="abreviatura" class="col-sm-2 control-label">Abreviatura</label>
            <div class="col-sm-10">
              <input type="text" class="form-control" id="abreviatura" name="abreviatura" value="{{data.ABREVIATURA}}">
            </div>
          </div>
            <div class="form-group">
            <label for="id_departamento" class="col-sm-2 control-label">Departamento</label>
            <div class="col-sm-10">
              <select class="form-control" data-style="btn-info" name="id_departamento" value="{{data.ID_DEPARTAMENTO}}">
                {{#each cmdData as |item index|}}
                  <option name="{{@index}}_cmb" value="{{./ID}}">{{./NOMBRE}}</option>  
                {{/each}}
              </select>
            </div>
          </div>
         <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <button type="submit" class="btn btn-default">Actualizar</button>
            </div>
          </div>
        </form>
      </div>
</div>

{{#if errors}}
  <div class="alert alert-danger" role="alert">
    {{#each errors}}
      <p>{{./msg}}</p>
    {{/each}}
  </div>
{{/if}}